#!/bin/bash

echo "#Deployment definition" > config
echo "home_domain=test.com" >> config
echo "sprout_hostname=sprout.test.com" >> config
echo "chronos_hostname=127.0.0.1:7253" >> config
echo "hs_hostname=hs.test.com:8888" >> config
echo "hs_provisioning_hostname=hs.test.com:8889" >> config
echo "ralf_hostname=ralf.test.com:10888" >> config
echo "xdms_hostname=homer.test.com:7888" >> config

echo "#Local IP Configuration" >> config
echo "local_ip=127.0.0.1" >> config
echo "public_ip=127.0.0.1" >> config
echo "public_hostname=$HOSTNAME" >> config
key="secret"
echo "# Keys" >> config
echo "signup_key=$key" >> config
echo "turn_workaround=$key" >> config
echo "ellis_api_key=$key" >> config
echo "ellis_cookie_key=$key" >> config
sudo mv config /etc/clearwater
