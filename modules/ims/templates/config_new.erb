#!/bin/bash
sudo mv /etc/clearwater/config /etc/clearwater/config_old_puppet
sudo echo "home_domain=test.com" > /etc/clearwater/config
sudo echo "public_hostname=$HOSTNAME" >> /etc/clearwater/config
sudo echo "signup_key=secret" >> /etc/clearwater/config
sudo echo "turn_workaround=secret" >> /etc/clearwater/config
sudo echo "ellis_api_key=secret" >> /etc/clearwater/config
sudo echo "ellis_cookie_key=secret" >> /etc/clearwater/config
sudo netconfd superuser=tcs module=ims &
sudo puppet agent --disable
